---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: faultdetections.detect.failure-recovery.io
spec:
  group: detect.failure-recovery.io
  names:
    kind: FaultDetection
    listKind: FaultDetectionList
    plural: faultdetections
    singular: faultdetection
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FaultDetectionSpec references a template and target object.
            properties:
              target:
                description: Target object (optional, based on Scope)
                properties:
                  apiVersion:
                    type: string
                  kind:
                    type: string
                  name:
                    type: string
                  namespace:
                    type: string
                type: object
              templateRef:
                description: Which template to use
                type: string
            required:
            - templateRef
            type: object
          status:
            properties:
              anomalous:
                type: boolean
              lastRun:
                format: date-time
                type: string
              nodeResults:
                items:
                  description: FaultDetectionStatus captures monitoring results.
                  properties:
                    message:
                      type: string
                    nodeName:
                      type: string
                    ok:
                      type: boolean
                  type: object
                type: array
              reason:
                type: string
              results:
                items:
                  description: Result stores metric query output
                  properties:
                    metric:
                      type: string
                    value:
                      type: string
                  required:
                  - metric
                  - value
                  type: object
                type: array
              triggerMsg:
                type: string
              triggered:
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
