apiVersion: detect.failure-recovery.io/v1alpha1
kind: FaultDetection
metadata:
  labels:
    app.kubernetes.io/name: detection-controller
    app.kubernetes.io/managed-by: kustomize
  name: faultdetection-sample
spec:
  # TODO(user): Add fields here
